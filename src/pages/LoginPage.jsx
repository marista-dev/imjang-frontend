// TODO: Claude Code에서 구현
// 연결 API: POST /api/v1/auth/login
// 레이아웃: 화면 중앙 카드 (max-w-sm), 이메일/비밀번호 input, 로그인 버튼
const LoginPage = () => {
  return (
    <div className="page flex min-h-screen items-center justify-center">
      <div className="card w-full max-w-sm">
        <h1 className="text-xl font-bold">로그인</h1>
        <p className="mt-2 text-slate-500">구현 예정</p>
      </div>
    </div>
  );
};

export default LoginPage;
