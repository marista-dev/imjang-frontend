/**
 * @deprecated Sonner로 대체됨.
 * 
 * 사용법:
 * import { toast } from 'sonner';
 * toast.success('성공 메시지');
 * toast.error('에러 메시지');
 * toast.warning('경고 메시지');
 */
export const useToast = () => {
  throw new Error('useToast는 Sonner로 대체되었습니다. import { toast } from "sonner"를 사용하세요.');
};
