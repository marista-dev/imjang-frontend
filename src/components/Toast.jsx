/**
 * @deprecated Sonner로 대체됨. main.jsx에 <Toaster />가 이미 설정됨.
 *
 * 사용법:
 * import { toast } from 'sonner';
 * toast.success('매물이 등록되었어요');
 * toast.error('등록에 실패했어요');
 * toast('일반 알림');
 *
 * 이 컴포넌트는 하위 호환을 위해 남겨둠. 새 코드에서는 직접 sonner를 사용.
 */
const Toast = () => null;

export default Toast;
